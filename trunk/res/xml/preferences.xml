<PreferenceScreen
        xmlns:android="http://schemas.android.com/apk/res/android">

   <CheckBoxPreference  
            android:key="check_updates"  
            android:defaultValue="false"
            android:title="Run in background?"  
            android:summary="Check for new tweets when app is not running"
            />  

   <ListPreference  
            android:key="check_update_interval"
            android:dependency="check_updates"  
            android:defaultValue="30"
            android:title="Update check interval"  
            android:summary="How many minutes between update checks"
            android:entries="@array/check_intervals"
            android:entryValues="@array/check_intervals"
            android:dialogTitle="Interval in minutes:"
            />  

</PreferenceScreen>